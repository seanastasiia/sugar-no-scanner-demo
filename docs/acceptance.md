# Acceptance matrix

Status reflects reproducible evidence in this repository, not the intended final result.

| Criterion | Current evidence | Status |
| --- | --- | --- |
| Direct camera-first entry | The root page opens the scanner without an access form and issues a silent 12-hour HTTP-only, same-site session cookie. Bare unauthenticated APIs remain rejected; the flow is public convenience, not viewer authentication | Pass locally |
| Official Sugar.no header brand | Scanner header uses the exact white symbol-and-wordmark SVG published on `sugar.no`, keeps its source aspect ratio, provides `alt="Sugar.no"` and makes no runtime request to the website or Framer CDN | Pass locally |
| 40 Latvia SKUs | Generated catalog validation and unit test | Pass |
| Broad Barbora discovery | Reproducible public-sitemap index contains 18,554 unique product slugs | Pass as discovery index |
| Active food source coverage | 9,707 active non-adult food SKUs; 7,433 exact pages (76.57%) have energy, protein and total sugar | Pass as catalog-data coverage, not visual accuracy |
| On-demand Barbora nutrition | An exact food SKU is fetched after recognition; unit tests require source-backed energy, protein and total sugar for the two-factor fit. Protein-only, sugar-only and identity-only states remain neutral | Pass locally |
| Exact Open Food Facts fallback | A visible barcode uses the official product-by-code API; strict text fallback requires matching brand, variant and pack size. Wrong Coke variant and wrong pack-size regressions fail closed | Unit pass; live barcode check pass |
| Automatic cited web nutrition | After local Barbora and exact Open Food Facts resolution fail, a high-confidence exact identity may receive Protein/Sugar only from a cited HTTPS per-100 source. A real Sproud query returns a complete fit; no citation, variant ambiguity or confidence below 0.90 fails closed. There is no user-facing label-scan action | Unit pass and live server-side query pass |
| Product outside scored catalog | Generic identity is implemented; the Sanpellegrino screenshot resolves to an exact Barbora page while non-exact candidates remain unlinked | Pass as concept |
| Camera occupies the primary viewport area | The live camera spans the full phone width and keeps the brand, demo action and recognition state as overlays. The media remains `object-fit: contain`, untappable and unfiltered; saved photos use one rounded 3:4 center-crop preview and demo scenes retain their designed frames | Pass locally |
| Result sheet expands into a page | Recognition opens a compact 158 px phone sheet whose rated product cards mirror the full ranking with position, fit and total sugar per 100 g or 100 ml; `View all` is its only compact action. The expanded sheet fills the viewport, hides background controls from focus and collapses back to camera | Pass locally |
| iPhone responsive matrix | Mobile Safari keeps the camera, compact actions, status and expanded dialog inside 402×874 iPhone 17 Pro portrait, 874×402 landscape, 440×956 large portrait and 375×667 small portrait viewports; only explicit product rails scroll horizontally | Pass locally |
| Saved-image multi-pass recognition | Dense landscape shelves and long portrait screenshots use one full-frame read plus three overlapping close-ups; ordinary portrait/focused uploads remain single-pass. Saved images may be online-grocery pages, where every readable product card is a candidate and online prices are never treated as physical shelf labels. Results are remapped, de-duplicated and opened as one vertical list; the preview stays a fixed rounded 3:4 center crop without misleading crop guides | Logic and Mobile Safari pass locally |
| Multi-product fit order | The expanded dialog renders a vertical best-first list. Complete Protein/Sugar fits sort from higher to lower score with visible fit text and values; confidently named unresolved identities remain visible as neutral unrated rows after a lookup miss. Anonymous price-only findings stay hidden. The four-item shelf and three-item checkout fixtures encode rated multi-product order; a saved-photo regression retains named misses | Pass locally |
| Uncertain broad frame stays honest | A broad camera frame that does not meet the recognition threshold remains `not_sure`; it does not launch a second full-image Gemini request or invent a center product. The captured frame remains visible until the user explicitly starts a new scan | Pass locally |
| At most ten products from one frame | Broad recognition scans the complete frame but returns no more than ten high-confidence distinct front-facing SKUs. Upload merge, API schema, prompt and resolver unit coverage enforce the same limit before any external nutrition enrichment | Pass locally; real-shelf precision remains to be measured |
| Repeated facings count once | Server and client normalize same-SKU detections; unit test merges four Coca-Cola descriptions and live-camera WebKit test renders one unique product | Pass locally |
| Captured result stays tied to its scene | The browser freezes the submitted stable frame as soon as `Reading…` begins. Recognition, overlays and background enrichment stay tied to that snapshot; phone movement does not replace the scene and a new Gemini request occurs only after an explicit new scan. Neutral pre-result candidates never imply identity or fit | Mobile Safari regressions pass locally |
| Camera result is not blocked by retailer lookup | Live camera skips the optional packshot-confirmation/retailer/OFF network path for its first response, returns local curated/index matches immediately after Gemini, then calls an image-free resolver in the background. A Mobile Safari regression holds that resolver open and still sees both named products in 1.5 s | Pass locally |
| Conflicting retailer brand | Coca-Cola/Pepsi brand regression is rejected before any offer is returned; possible matches have no UI link | Pass locally |
| Price requires shelf label | Unit regression requires a separate-label signal, confidence ≥0.90 and matching EUR text; browser regression confirms no price module without that evidence and no retailer link for a non-exact SKU | Pass locally |
| Shelf demo shows cheaper-online concept | The first deterministic shelf result labels €3.49 as a demo shelf value, crosses it out only against an exact €2.79 Barbora offer checked on 25 August 2026, and exposes the exact one-tap retailer link in both compact and expanded views | Pass locally |
| Camera focuses on Sugar.no ratings | Complete protein-plus-sugar fits use `Great / Moderate / Low` markers. One-factor and identity-only detections receive no camera marker but remain as neutral named rows; anonymous price-only detections are removed after lookup | Pass locally |
| Camera overlay stays visually focused | Rated markers show the fit icon and selected fit label without a redundant `2/2 signals` pill. Selected and best packages keep their fit-colored outline and never add a white frame. Green, yellow and red fit discs all render at the same 46 px visual size while the full product box remains the larger touch target | Pass locally |
| Unrated result stays honest | A failed exact nutrition lookup keeps a confidently named product as a neutral unrated row, without a fit or invented nutrition. Anonymous price-only findings and generic placeholders are omitted | Pass locally |
| Ranked comparison hierarchy | The ranked list is the single comparison surface. It begins with `Best fit first`, carries rank/fit/nutrition in each row and never repeats a separate `Best fit in this scan` block | Pass locally |
| Nutrition never invented by AI | Main recognition hydrates curated, exact Barbora, exact Open Food Facts or a cited exact web record. Grounded web output is rejected without an exact identity, HTTPS grounding source, per-100 basis and confidence at least 0.90 | Pass |
| Badge calculation and missing-data rule | Unit tests cover the equal protein/inverse-sugar formula, fiber independence, category percentiles, EU-reference bands, solid/liquid sugar thresholds, one-factor neutrality, zero-factor identity, adult pages and no-data pages | Pass |
| Exact Barbora link and current price | Live page parser, brand guard, candidate-margin tests and UI end-to-end assertions allow only an exact SKU to drive the `Cheaper at Barbora` action or crossed-out shelf price | Pass locally |
| Better-alternative purchase actions | Every candidate is the same exact product type, has an equal or better fit and an active exact Barbora offer. Each card owns its price and `Buy online` link; the section stays hidden when no true substitute exists | Unit and Mobile Safari E2E pass locally |
| Shelf demo | One photorealistic concept image returns four deterministic detections with compact fit markers and a collapsed ranked thumbnail sheet; the full best-first comparison appears on expansion | Pass as concept |
| Checkout demo | One real checkout-belt photo returns the three packaged identities Gemini could read (Sproud, Schnitzer and Stockmann), three fit-coloured camera overlays and a three-row best-first list. Selecting it from an actively scanning live camera aborts the stale camera request before deterministic recognition starts, so a late focus retry cannot overwrite the demo. Sproud and Schnitzer use official manufacturer nutrition; chanterelles use an explicitly labelled generic raw-food composition reference | Mobile Safari race regression and controlled production active-camera transition pass: one checkout request, three persistent markers, no stale focus retry |
| Shelf/checkout navigation | `Show demo` opens shelf, checkout and saved-photo choices; every demo has a clear return to live camera | Pass locally |
| No save actions | Product, checkout and Better alternatives results expose comparison and retailer actions without Save buttons or a persisted product list | Pass locally |
| Repeated-frame de-duplication | Unit tests retain one tray entry per SKU across repeated live-camera detections | Pass as logic |
| Raw images not stored in analytics | `imageStored: false`, metadata guard and API end-to-end rejection test | Pass at app boundary |
| Permission denied | Mocked WebKit browser scenario verifies explanation, retry and continued access to `Show demo` | Pass locally |
| Saved-image fallback | Client resize and provider-unavailable state tested | Pass locally |
| Shelf-price comparison | UI regression proves a trusted €1.69 shelf price is crossed out inside the matching product row only for an exact €0.99 Barbora match; the former standalone price-comparison block is absent | Pass as logic |
| Per-card online purchase | Every exact current retailer offer owns its full-width `Buy online` action inside the matching expanded product card. The action becomes green and says `Buy cheaper online` only when a trusted physical shelf price is higher; possible matches expose no action | Pass locally |
| Live shelf-label OCR | Real Gemini call on the concept shelf identified six distinct front-facing products and associated €1.79/€2.29 labels in 7,159 ms; physical Latvia benchmark is absent | Partially measured |
| Live recognition rate limit | Default 36/60 allowance covers the roughly 29/min camera cadence; samples are exempt and API `429` includes `Retry-After`. Client code pauses capture, preserves a provisional product and exposes retry/demo actions; unit and browser regressions encode the contract | Pass locally |
| Production build | `next build` succeeds and standalone assets are prepared | Pass locally |
| Focus top-1 ≥ 90% on 10 real packshots | Gemini is configured and one production concept-image call succeeds; the 10-packshot benchmark has not been run | Not measured |
| Shelf recall ≥ 75% on real/golden images | Deterministic demo is not an accuracy benchmark | Not measured |
| Checkout recall ≥ 80%, duplicates ≤ 5% on a real belt | Dedup logic passes; real photo/video vision benchmark absent | Partially measured |
| Unsupported false-positive rate ≤ 5% | Closed-catalog fail-closed behavior exists; real negative set absent | Not measured |
| Cached result < 1 s | Deterministic/API flow observed in WebKit | Pass locally |
| New AI p95 < 4 s | Live camera no longer waits for packshot confirmation, live retailer pages or Open Food Facts before showing the first result; server timing logs split Gemini provider time from total time. One browser regression exposes two identities while enrichment remains pending, but a physical-device production p95 sample has not been collected | Not measured |
| Supabase migration, seed and writes | Migration and seed are reproducible | Prepared, not applied |
| Railway HTTPS and live health check | Railway deployment `3290d309-d48e-496f-a62e-92c4287a5590` succeeded for commit `4d9a0ef79df7542ce1bc93c56d7bb4395d8eb1a1`; `/api/health` reported the same commit, `/` returned HTTP 200, Shelf returned four products and Checkout returned three products | Pass in production |
| Physical iPhone Safari shelf test | Requires deployed HTTPS and physical products | Not run |

The deterministic scenes prove the interaction and data contract. They must not be presented as evidence of real-world computer-vision accuracy. Current release evidence is in [the project cleanup run](test-runs/2026-08-26-project-cleanup.md); historical QA logs remain available in Git history.
