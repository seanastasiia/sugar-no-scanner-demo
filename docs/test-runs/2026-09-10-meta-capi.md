# Meta server Purchase verification

Base: 56f87c0. Technical plan: closed-payload and consent unit tests; executable PGlite migration/role/lease/expiry tests; signature and retry route tests; full verify, full browser suite, Meta-enabled browser regression and public SDK contract. Live acceptance requires a permitted Meta server token and the additive Supabase migration, followed by a Server Purchase in Test Events and matching browser/server deduplication without new real payment.

Owner product check: rejected consent leaves Scanner usable and sends no Meta event; granted consent permits payment measurement; withdrawal suppresses further server/browser sends; re-opening the existing paid return preserves access and does not count another purchase.

Initial targeted run: 31 tests passed across six files, including actual SQL execution. Meta UI currently refuses token generation: “You must be an admin or developer for this business portfolio to create an access token.” Pixel owner shown as Anastasiia Dedova / personal ad account 1436269865027465. Supabase browser session needs owner login. Neither blocker has been bypassed and CAPI is not enabled.

`npm run verify` passed twice, last run 91 files / 777 tests plus lint, typecheck, catalog validators and production build. Four additional consent-endpoint tests passed separately. The enabled-Meta production browser run passed all four cases (7.1 s). Initial full suite found two stale acceptance assumptions from prior changes: old absence of nutrition rows and a contrast snapshot during entrance opacity. Corrected the tests without product styling changes and reran the full suite. Raw logs are retained under ignored `test-results/meta-capi-release/` after the final run.

The owner Mac became locked during browser work. No attempt was made to bypass login or Meta portfolio permissions. The feature remains off unless both migration and token are supplied; billing and browser Pixel continue without them.

Final verification: full Mobile Safari suite 71 passed (2.4 min), four Meta-disabled cases skipped by design and separately passed with Meta enabled. Real public SDK contract passed with locally intercepted PageView, Purchase and OnboardingCompleted plus withdrawal. Added consent-route tests: 4/4; final typecheck and touched-file lint passed. No real payment or production QA event was created. Production activation remains blocked by Meta token permissions and Supabase login.
