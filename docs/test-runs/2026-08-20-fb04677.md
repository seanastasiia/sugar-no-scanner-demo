# Production release verification: fb04677

- Tested commit: `fb046773d5eae42cee19116dcb28dfb098c73834`
- Time: 2026-08-20 13:38 EEST
- GitHub: `https://github.com/seanastasiia/sugar-no-scanner-demo`
- Railway: `https://sugar-no-scanner-demo-production.up.railway.app`
- Railway deployment: `499d318d-13d9-425f-9e80-3fe9ef9e8add`

## Local technical checks

Command:

```bash
npm run verify
E2E_PRODUCTION=1 CI=1 npm run test:e2e
```

Result: pass.

- ESLint: pass
- TypeScript: pass
- Vitest: 5 files and 18 tests passed
- Next.js standalone production build: pass
- Playwright WebKit with iPhone 13 profile: 7 scenarios passed with no retry

## GitHub and Railway release

- Public GitHub `main`: pass; tested commit is present on the remote.
- Railway source: `seanastasiia/sugar-no-scanner-demo`, branch `main`.
- Railway deployment status: `SUCCESS`.
- `/api/health`: HTTP 200 with service name and the exact GitHub commit SHA.
- Railway domain and PWA manifest/icon: HTTP 200.

The first two deployment attempts failed because the standalone Next.js server bound to Railway's container hostname. Production now sets `HOSTNAME=0.0.0.0`; the corrected GitHub deployment passed the platform health check.

## Live production smoke

Result: pass.

- unauthenticated `/`: HTTP 307 to `/access?next=%2F`
- wrong investor code: HTTP 401
- correct investor code: HTTP 200 with a private session cookie
- authenticated home: HTTP 200
- deterministic shelf API: four supported detections, `imageStored: false`
- deterministic conveyor API: five frames collapse to four unique SKU IDs
- product details: sourced nutrition, Match, two alternatives and an exact Barbora URL
- unsafe analytics metadata containing an image data URL: HTTP 400 `unsafe_event_metadata`

## Not yet verified

- live camera recognition against Gemini because `GEMINI_API_KEY` is not configured
- Supabase migrations, seed and writes because no Supabase project is connected
- physical products, real shelf/conveyor conditions and iPhone camera permission on the deployed domain
