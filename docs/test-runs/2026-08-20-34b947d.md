# Scanner UX release verification: 34b947d

- Tested commit: `34b947dd260c640b8ffdcc7fbc37fc1a361e3fb9`
- Time: 2026-08-20 14:04 EEST
- GitHub: `https://github.com/seanastasiia/sugar-no-scanner-demo`
- Railway: `https://sugar-no-scanner-demo-production.up.railway.app`
- Railway deployment: `b62fb18c-acb5-4a8a-baf5-626f7ba8a152`

## Local technical checks

Commands:

```bash
npm run verify
E2E_PRODUCTION=1 CI=1 npm run test:e2e
```

Result: pass.

- ESLint: pass
- TypeScript: pass
- Vitest: 6 files and 22 tests passed
- Next.js standalone production build: pass
- Playwright WebKit with iPhone 13 profile: 8 scenarios passed with no retry
- Responsive smoke: pass at 375 × 812 and 812 × 375 with no document-level horizontal overflow
- Automated WCAG A/AA check on the entry experience: no violations

## Product-flow evidence

- Shelf sample: one image returns four supported products and four unique IDs.
- Checkout sample: one full-belt image returns four supported products and four unique IDs through the same recognition API.
- Red, yellow and green detection overlays are rendered directly over both scenes and repeat the meaning with `Strong`, `Middle` or `Lower` text.
- The primary result has no numeric Sugar.no Match; it shows Protein, Fiber and Sugar criteria separately.
- Similar options use a horizontal scroll-snap card row with a partially visible next item.
- Updated mobile screenshots are stored in `docs/screenshots/shelf-mobile.png` and `docs/screenshots/checkout-mobile.png`.

## GitHub and Railway release

- Public GitHub `main`: pass; tested commit is present on the remote.
- Railway source: `seanastasiia/sugar-no-scanner-demo`, branch `main`.
- Railway deployment status: `SUCCESS`.
- `/api/health`: HTTP 200 with `status: ok` and the exact tested commit SHA.
- Authenticated live shelf API: four detections, four unique IDs, `imageStored: false`.
- Authenticated live checkout API: four detections, four unique IDs, `imageStored: false`.

## Not yet verified

- live camera recognition against Gemini because `GEMINI_API_KEY` is not configured
- Supabase migrations, seed and writes because no Supabase project is connected
- physical products, real shelf/checkout-belt conditions and iPhone camera permission on the deployed domain

