# Gemini production enablement · 2026-08-20

## Release under test

- Code commit: `0b82e72866b04eafe2dffcfdf64e18e93625f2de`
- Railway deployment: `8211684e-8fb4-428e-8568-398ab7ccf22c`
- Production: `https://sugar-no-scanner-demo-production.up.railway.app`
- Secret handling: the credential was never printed, committed or exposed to browser code; the check confirmed only that Railway had a non-empty 53-character server secret.

## Technical checks

| Check | Result |
| --- | --- |
| Gemini API enabled and key restricted/bound to dedicated service account | Pass |
| Gemini model discovery | HTTP 200; `gemini-3.7-flash` present |
| Minimal Gemini generation/quota check | HTTP 200; 10 total tokens reported |
| Full closed-catalog schema without provider enum | HTTP 200 on the concept shelf image |
| `npm run verify` | Pass: lint, typecheck, 26 unit/integration tests, production build |
| Enlarged-text WebKit regression repeated five times | 5/5 pass |
| `CI=1 npm run test:e2e` | 10/10 Mobile Safari tests pass |
| Railway deployment | `SUCCESS` |
| `GET /api/health` | HTTP 200; exact commit above |
| Authenticated production `POST /api/recognize` with the concept shelf image | HTTP 200; `matched`; 3 detections; `gemini-3.7-flash`; 2,658 ms; `imageStored: false` |

The first production attempt returned HTTP 502 because Gemini rejected the 40-value product-ID enum in the JSON schema as `INVALID_ARGUMENT`. The provider schema now accepts a string while the server-side 40-product allowlist remains authoritative and has a regression test proving that unknown IDs are discarded.

## Product checks for Anastasiia

1. Open the private production link on iPhone Safari and grant camera access.
2. Point at the front of one of the 40 catalog protein snacks for several seconds.
3. Confirm that the old `Live recognition needs the Gemini key` banner does not appear.
4. Confirm that a confident known product opens a badge, while an unsupported item says `Not sure` rather than guessing.
5. Repeat with a shelf or checkout view; the physical-product accuracy benchmark is still outstanding.
