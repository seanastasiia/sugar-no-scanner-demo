# Test run: db76260

- Commit: `db76260df4103d4388278a4dc66b34c056e3f5ad`
- Time: 2026-08-20 13:14 EEST
- Host: macOS arm64, Node.js 24.18.0, npm 11.16.0
- Browser runner: Playwright WebKit 26.5 with iPhone 13 profile

## Catalog

Command:

```bash
CATALOG_CHECKED_AT=2026-08-20 npm run catalog:sync
npm run catalog:validate
```

Result: pass. 40 unique Latvia products, 10 with complete Match nutrition, 30 intentionally pending independent fiber verification.

## Static, unit and build verification

Command:

```bash
npm run verify
```

Result: pass.

- ESLint: pass
- TypeScript: pass
- Vitest: 5 files, 18 tests passed
- Next.js production build: pass
- Standalone artifact preparation: pass
- Routes: access, health, auth, recognize, products and events compiled
- Proxy middleware: compiled

## Production Mobile Safari scenarios

Command:

```bash
E2E_PRODUCTION=1 CI=1 npm run test:e2e
```

Result: 7 passed in 15.1 seconds, no retries.

1. Private gate rejects wrong code and accepts the configured code.
2. Shelf scene returns four products, Match inputs and an exact Barbora URL.
3. Checkout scene builds four unique SKUs and suppresses the repeated SKU.
4. Recognition declares `imageStored: false`; analytics rejects image data.
5. Camera permission denial shows explanation and retry.
6. Saved image is resized client-side and fails closed without Gemini.
7. Entry screen has no automated WCAG A/AA violations in axe-core.

Visual evidence:

- `docs/screenshots/access-mobile.png`
- `docs/screenshots/shelf-mobile.png`
- `docs/screenshots/checkout-mobile.png`

## Standalone health/security smoke

The built `.next/standalone/server.js` was started on port 3100 with test-only access variables.

Result:

- `/api/health`: 200 and correct service/commit payload
- unauthenticated `/`: 307 to `/access?next=%2F`
- unauthenticated `/api/recognize`: 401
- public PWA PNG icon: 200 `image/png`
- correct access code: 200 with HttpOnly cookie
- authenticated deterministic shelf recognition: 200, four detections, `imageStored: false`
- security headers present: `X-Content-Type-Options`, `Referrer-Policy`, `Permissions-Policy`, `Cross-Origin-Opener-Policy`

## Not executed

- `npm run benchmark:focus`: blocked because `GEMINI_API_KEY` is not configured.
- Supabase migration/seed/write verification: blocked because no Supabase project/credentials are configured.
- GitHub push and Railway deployment/health check: blocked because GitHub and Railway are not authenticated on this machine.
- Physical iPhone/shelf/conveyor accuracy: requires the deployed HTTPS URL and real test products.
