# Ranked v11 production release, 25 September 2026

Owner explicitly approved publishing the reviewed ranked variant and starting the activation comparison. The same conversation approved the official Railway CLI connection scoped to the current workspace/project and a separate €30 advertising extension (lifetime cap €150, end 28 September 23:59 Riga).

## Release and automated checks

PR [#1](https://github.com/seanastasiia/sugar-no-scanner-demo/pull/1) merged as `81b8c0b1a5b0ef45f63e2a9ad04951191bb0dee5`. Runtime was verified at `5cbfeb0` before documentation and read-only SQL changes: 842 Vitest tests, types, catalog checks and production build passed; full CI browser suite passed 72 with 12 feature-gated skips. Optional SQL version-filter fixture passed. Detailed commands/logs: [activation test](../scanner-first-value-v11.md).

Direct Railway deployment `8eec5f42-d919-49f9-8b5c-89b72d54db3c` reached SUCCESS. Its predecessor from GitHub autodeploy was superseded by this same-source CLI deployment. `/api/health` returned status=ok and the exact main SHA at `2026-09-25T11:13:35.632Z` (14:13:35 Riga). Capture: `/tmp/scanner-v11-production-health.json`. The documentation-only follow-up does not change runtime code; verify the final deployed main SHA in the shared dated release record.

## Production smoke

- Root returned HTTP 200; public session established normally. Bare event request without session was rejected with 401.
- Signed-session deterministic shelf request returned HTTP 200, four detections, `imageStored=false`.
- Built-in Browser production view at 390×844 showed the approved ranks 1–4, leader packshot and visible primary action. Screenshot: `/Users/anastasiia/.codex/artifacts/shelf-scanner/v11-production-onboarding-2026-09-25.png`.
- QA browser URL explicitly used `qa=1`, campaign label and `utm_content=v11_release_qa`. Primary action reached the camera-unavailable state on this Mac environment. Show demo → Use saved photo uploaded the repository's already-public `public/samples/latvia-shelf.jpg` through the real provider path, not the deterministic sample route. The UI displayed five ranked identified products. Screenshot: `/Users/anastasiia/.codex/artifacts/shelf-scanner/v11-production-real-upload-2026-09-25.png`.
- Railway's four upload passes returned 3/7/8/8 detections, approximately 6.8/9.2/11.5/13.0 seconds total per pass. These are provider-pass counts, not unique final products or customer visits. Bounded evidence: `/tmp/scanner-v11-recognition-qa.json`.
- One synthetic `app_opened` event was explicitly QA-labelled with version 11. Production endpoint returned HTTP 200, `ok=true`, `storage=supabase`, `analytics=sent`. No synthetic success/purchase was recorded. Capture: `/tmp/scanner-v11-event-storage-qa.json`.
- Direct readback of the browser visit in authorized Safari/Supabase was temporarily blocked by the Mac lock; the owner was asked to unlock. Do not confuse the API storage acknowledgement with direct database readback of every browser event.

## Advertising and measurement

Meta confirmed one campaign and one ad set updated. Published budget is €150 lifetime, and readback shows 28 September 2026 23:59 Riga. Latvia 18+, LPV optimization and the three active Video A/Static C/Static D creatives remain; Static B is Off. The six previous drafts remain unpublished. Immediately after publication the ad set was Processing; the three ads showed Active.

The existing `shelf-scanner-gemini` heartbeat was updated in place to compare version 11/10, preserve QA/sample exclusions, separate the partial launch day and summarize full test days 26–28 September in the 29 September report. ID, ACTIVE state, existing task binding and daily 10:00/11:00 Riga schedule were verified unchanged. This is sequential before/after, not randomized A/B. Main metric is completed real camera/upload scans per campaign tab visit; a few visits cannot establish improvement.

## Remaining owner product checks

1. Open `https://sugar-no-scanner-demo-production.up.railway.app/?onboarding=1&qa=1` from Instagram and Facebook on a physical phone. Confirm the ranking is immediately readable and tap Compare → allow camera → scan shelf → open result.
2. Try a saved JPEG and a scene without packages; the latter should offer retry without consuming a successful-scan allowance. Note Riga time and browser/OS for correlation.
3. The camera-error screen currently hides saved-photo upload behind Show demo, despite suggesting a saved photo. Tracked in Bugs.md as a follow-up. The missing camera on this Mac is not evidence of a physical-phone defect or the cause of September 24 abandonment.

No blanket claim that every recognition, catalog value or embedded-browser camera path has been validated. No purchase was made, no Stripe reconciliation was performed, and no customer conversion improvement is claimed at launch.
